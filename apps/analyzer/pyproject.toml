[project]
name = "wedge-analyzer"
version = "0.1.0"
description = "Minimal scaffold for the Wedge analyzer service."
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "pika",
    "prometheus-client==0.23.1",
]
